stage('Run Python Script') {
    steps {
        script {
            sh 'pwd'  // Print the current working directory
            sh '. venv/bin/activate && python path/to/main.py'  // Specify the correct path to main.py
        }
    }
}
